import React from "react";

const header = () => {
  return (
    <nav>
      <a href="#">Home</a>
      <a href="#">My Account</a>
      <a href="#">Pokedex</a>
      <a href="#">Help</a>
    </nav>
  );
};

export default header;
